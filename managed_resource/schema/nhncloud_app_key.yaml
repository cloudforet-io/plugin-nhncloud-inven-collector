---
schema_id: nhncloud-app-key
name: NHN Cloud App Key
schema_type: SECRET
schema:
  order:
  - app_key
  - email_secret_key
  - push_secret_key
  properties:
    app_key:
      format: password
      title: APP Key
      type: string
    email_secret_key:
      format: password
      title: Email Secret Key
      type: string
    push_secret_key:
      format: password
      title: Push Secret Key
      type: string
  required:
  - app_key
  type: object
provider: nhncloud